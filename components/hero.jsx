export default function Hero() {
    return(
        <section className="relative w-full min-h-[80vh]">
            <div className="absolute inset-0 -z-10 bg-grid-img" />

            
        </section>

    )
}